window.addEventListener("load", function () {
    let mainForm = document.getElementById("mainForm");

    mainForm.addEventListener("submit", function (event) { //submit
        // event.preventDefault();
        // alert("didn't make it");
    });
    console.log(mainForm);
});
